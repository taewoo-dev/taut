[project]
name = "taut-architecture-demo-after"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["fastapi==0.116.1"]

[tool.pyright]
typeCheckingMode = "strict"
include = ["app"]

[tool.taut]
extend = "../policy.toml"
