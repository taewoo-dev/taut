# pytaut 0.2.0 release gate report

**Decision: READY.** The exact anti-monitor checkout was analyzed read-only and produced
`requested=952 discovered=952 complete=952 failed=0 partial=0 engine_issues=0`. No history, tags,
remotes, external repositories, or `logs/` were changed.

## Environment and commit sequence

- Platform: macOS 26.5.1 arm64; Python 3.14.0 (`uv` isolated and project environments).
- Release metadata: `pytaut` 0.2.0, `requires-python >=3.12`, lockfile synchronized.
- Commits audited, newest first: `f88a258` architecture-map documentation integration; `63aab2d`
  release docs/migration; `374636e`, `1759447`, `3c080dd`, `b5167c5` entry-point compatibility;
  `f69c8ef` plugin contracts; preceding R1/R2 policy, performance, and uncertainty commits.
- Workspace status: only pre-existing untracked `logs/` is present.

## Executed gates

| Gate | Result |
|---|---|
| `git diff --check` and workspace ownership audit | PASS |
| `bash scripts/test.sh` (conventions, Ruff, mypy, Pyright, self-policy) | PASS |
| Full pytest + branch coverage | PASS: 935 passed, 90.04% (threshold 90%) |
| `tests/unit/test_performance_contracts.py`, contract, integration tests | PASS |
| `uv run python scripts/benchmark_performance.py --real-checkout /Users/taewoo/orca/workspaces/anti-monitor/backend-taut-compliance/backend --requested 952 --scale small --generic` | PASS: requested 952, discovered 952, complete 952, partial 0, failed 0, engine issues 0 |
| sdist and wheel build | PASS: `pytaut-0.2.0.tar.gz`, `pytaut-0.2.0-py3-none-any.whl` |
| isolated wheel install/import | PASS: version `0.2.0` |
| isolated CLI/version/rules/config/report smoke | PASS |
| installed built-in/plugin smoke | PASS: backend pack 48 rules; 4 built-in providers |
| repeated JSON output and digest comparison | PASS: byte-identical repeated JSON |

The exact real-checkout run took 432.18 seconds, read 952 files, and reported 2.20 sources/second,
633,995,264 bytes peak RSS delta, 0 analysis issues, 0 engine issues, snapshot digest
`db4b548939566349d1ea39a62b68915e9d052bb78eed9943a69478b7b229bd63d`, and policy digest
`1f1226e651d03a0e5294c95c8cf602a00eaa01fdbbf92f6d7e9f1b633f85c48d`. The synthetic performance
run remained deterministic with snapshot digest
`315564516a22d0847d3ff04f13ede8bef7f6cd170fa39f78424982dc05241495`.

## Known non-blocking caveats and next action

The installed wheel has no third-party entry points because this distribution does not declare a
fixture plugin package; built-in provider and rule-pack loading was verified. The anti-monitor run
reported 2,469 exit-relevant indeterminate evaluations; these are expected coverage evidence, not
engine failures, and are preserved in the report output.

Rollback remains the documented `pytaut==0.1.*` environment/configuration path in
[`MIGRATION.md`](../../../MIGRATION.md); no publish, tag, or push was performed.
