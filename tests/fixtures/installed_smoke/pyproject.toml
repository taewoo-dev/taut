[project]
name = "taut-installed-smoke"
version = "0.0.0"

[tool.taut]
schema_version = 3
packs = ["taut.backend"]
providers = ["taut.python-core"]
source_roots = ["."]

[tool.taut.roles]
service = ["app/*.py"]

[tool.taut.allow]
service = ["service"]
